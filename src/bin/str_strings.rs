#![allow(unused)]

fn main() {
  //String is a struct
  //pub struct String {
  //  vec: Vec<u8>
  //}
  // Owned
  // Mutable and growable
  // Allocated on the heap
  // &String can be coerced into &str


  //str - string slice
  //Dynamically sized type / unsized type
  //size of type not known at compile time


  //&str
  //size known at compile time (pointer)
  // immutable borrow


  //&mut str
}